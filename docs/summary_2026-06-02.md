# ViT 기반 MRI 재구성 — 전체 문서 통합 정리 (2026-06-02)

> 이 문서는 프로젝트의 모든 마크다운 문서(docs/ 19개 + CLAUDE.md + README.md + PROJECT_SUMMARY.md + v7/README_v7.md)를
> **2026-06-02 시점**으로 통합·정리한 마스터 요약본이다. 발견한 **문제점은 §6 에 별도 정리**.
> - v1→v6_3 상세 발표본: [presentation_overview.md](presentation_overview.md) (2026-05-28, v6 트랙까지)
> - 평가 재설계 상세: [eval_metric_redesign.md](eval_metric_redesign.md)
> - v7_titan 재학습 상세: [ss2d_v7_titan_changes.md](ss2d_v7_titan_changes.md)
> - 날짜순 인덱스: [INDEX.md](INDEX.md)
>
> **2026-06-02 갱신**: U-Net checkpoint 복원·검증(496.4M, chans=256); §6 에서 best-val/full-eval 정합 + "복원/향상 단계" 프레이밍 반영.

---

## 0. 한 줄 현황

- **v6 (320) 트랙 [완료]**: SS2D-ViT v6_3 가 pretrained U-Net(raw SSIM 0.8858) 대비 **SSIM 추월(0.8924 vs 0.8858, 견고)**. PSNR 은 best-val 36.05 이나 **full-eval 기준 마진은 작을 것**(§6-②). SS2D > ETER 일관.
- **v7_titan (384) 트랙 [진행 중]**: ETER 50ep 완주 (masked composite **0.9127** / SSIM_m **0.9084**). SS2D 재학습 중 — 2026-06-02 **epoch 7/50**, 완료 예상 **~6/13**.
- **오늘의 신호**: SS2D v7_titan이 **epoch 6 (SSIM_m 0.8979) 에서 이미 ETER epoch 10 (0.8966) 을 추월** → v6의 "Mamba > GRU" 패턴이 384에서도 조기 재현 중.
- **핵심 미해결 질문**: SS2D가 384에서 ETER ep50(0.9084)을 끝까지 넘는가.

---

## 1. 프로젝트 정의

| 항목 | 값 |
|---|---|
| 데이터 | fastMRI brain multicoil (16코일, real/imag 32채널 입력) |
| 컨트라스트 | "AXFLAIR" 표기되나 실제 혼합(AXT1/POST/PRE/AXT2/FLAIR) — CLAUDE.md 표기 부정확 |
| 가속 | R=4 equispaced, center fraction 0.08 |
| 입력/출력 | 언더샘플 k-space + aliased 이미지 → 단일채널 magnitude |
| 구조 | ViT 인코더 + 시퀀스 디코더(**GRU=ETER** / **Mamba=SS2D**) |
| 베이스라인 | fastMRI pretrained U-Net (chans=256, 496.4M). **checkpoint 2026-06-02 복원·검증 완료**(`models/pretrained/`, §6-①). 0.8858 은 v6 시절 raw SSIM 기록 |
| 환경 | conda `mri_env`, PyTorch + mamba_ssm 2.2.2 |

### 공통 파이프라인
```
입력: aliased image (B,32,H,W) + k-space (B,32,H,W)
   ViT 인코더(공통) ──┐                  ┌── 시퀀스 모델
                      ViT 디코더(공통)    ETER: Bi-GRU(수평→수직)
                      final_linear        SS2D: 4-방향 SSM(+1-iter soft DC block)
                          └──── concat ────┘
                                  │  최종 합성 Conv/RefinementBlock
                              출력 (B,1,H,W)
```
- Loss: `L1 + λ·(1−SSIM)` (λ: v1=0.2 → v2~=1.0). Loss는 fp32(autocast 밖) — label≈959, SSIM 제곱이 fp16 max 초과 방지.
- Optimizer: Adam lr=2e-4, weight_decay 1e-7→(v4)1e-5→(v5~)3e-5.
- Scheduler: (v3~) CosineAnnealingLR 단조 감소(WarmRestarts 톱니 폐기).

---

## 2. 세 갈래 트랙 개관

| 트랙 | 해상도 | ViT 인코더 | SS2D 용량 | GPU | 평가 척도 | 상태 |
|---|---|---|---|---|---|---|
| **v6** | 320 | ViT-Small (384/L6/H6) | d_inner64/d_state16 | RTX 5060Ti **8GB** | raw skimage SSIM | **완료** (SS2D v6_3 채택후보) |
| **v7** | 320 | ViT-Small | 동일 | TITAN RTX 24GB×2 | (재설계 전) | capacity 복원 마이그레이션 — v7_titan에 의해 대체 |
| **v7_titan** | **384** | **ViT-Base (768/L12/H12)** | **d_inner128/d_state32** | TITAN RTX **단일** | **brain-mask masked composite** | **운영 버전** (ETER 완주 / SS2D 진행 중) |

> 버전 계보: v4 → (v5 미완료) → v6 → v7 → **v7_titan(현재)**. v7_titan 파라미터 ≈ **114.8M**.
> 인코더/디코더 클래스는 무변경 import(`choh_ViT`, `choh_Decoder_SS2D_ViT`).
> **⚠ 척도 차이**: v6=raw SSIM(복원 단계), v7_titan=masked SSIM(향상 단계). 두 트랙 절대수치 직접 비교는 **무의미**(버그 아님 — 단계 경계, §6-④).

---

## 3. v6 (320) 트랙 — v1→v6_3 변천사

### 3.1 버전별 핵심
| 버전 | 날짜 | 핵심 변경 | best val SSIM |
|---|---|---|---|
| v1 | 04-09 | 8GB 축소 baseline (patch32, Conv 1개, SSIM w0.2) | 0.599 (custom) |
| v2 | 04월 | patch16(400토큰), RefinementBlock, SSIM w=1.0 | 0.759 (custom) / PSNR 36.38 |
| v3 | 04-20 | scheduler WarmRestarts→CosineAnnealingLR | ~0.74 (custom) |
| v4 | 04-22~27 | A:SS2D 증설 + B:reg(wd/dropout) + C:1-iter soft DC | SS2D 0.7340 / ETER 0.7320 (custom) |
| v5 | 04-30 | 데이터 +67%(crop/pad, val 7270=U-Net셋) + flip aug + EarlyStop | 0.7227/0.7421 (custom) ≈ **0.8584/0.8469** (skimage) |
| **v6** | 05-04~11 | **val SSIM custom→skimage**, EarlyStop val_ssim 단일, v5 best resume | **SS2D 0.8913 / ETER 0.8862** (skimage) |
| v6_1 | 05-20 | +gradient L1 loss λ=10 | SS2D 0.8953 / **PSNR −0.63dB (over-sharp 회귀)** |
| v6_2 | 05-25 | λ_grad 10→3 | 동일 trade-off, 폐기 |
| **v6_3** | 05-28 | dropout 0.2→0.1, WD 3e-5→1e-5 (gradient loss 제거) | **SS2D 0.8924 / 모든 지표 ↑ (채택후보)** |

### 3.2 두 차례 핵심 발견
1. **발견 ① custom SSIM 버그 (05-04)** — `u_choh_SSIM.py` 의 `val_range=None`→`L=1` 분기. fastMRI raw 값<1이라 SSIM이 인위적으로 낮게 측정됨. skimage 표준으로 재평가 시 v5가 0.7227→**0.8584**로 도약, **SS2D > ETER 로 ranking 반전**, U-Net 갭 0.15→0.027.
2. **발견 ② visual-metric gap (05-20)** — SSIM 0.89인데 시각적으로 흐릿. 원인 4: (1) raw amplitude SSIM의 배경 부풀림(배경 50%+), (2) 비교 슬라이스 below-average 편향, (3) **L1+SSIM의 mean-prediction blurring(본질적)**, (4) 에러맵 colormap 조기 saturation.

### 3.3 Sharpness 회복 실험
- **Tier 1 (TTA/앙상블, 무학습)**: 4-way flip TTA·cross-arch 앙상블 모두 임계(+0.003 SSIM/+0.15dB) 미달 → **전부 negative** (v6가 이미 flip aug 학습됨).
- **Tier 2 (fine-tune)**: gradient L1(v6_1/v6_2)은 항상 PSNR 회귀(over-sharpening) → **edge L1 loss 부적합 확정**. regularization 완화(v6_3)만 작지만 정직한 개선(첫 non-degradation).
- 남은 카드: **v6_4 (VGG perceptual loss)** — config/helper 준비, 미착수.

### 3.4 v6 종합 (raw skimage SSIM, **full-eval 7270 기준**)
| 모델 | SSIM | PSNR(dB) | NMSE | L1 |
|---|---|---|---|---|
| U-Net (PT, baseline) | 0.8858 | 34.66 | 0.00737 | — |
| SS2D v6 | 0.8913 | 34.81 ± 2.78 | 0.00906 | 7.37 |
| ETER v6 | 0.8862 | 33.59 ± 2.74 | 0.01143 | 8.21 |
| **SS2D v6_3 (★)** | **0.8924** | best-val 36.05 ‡ | — | 7.30 |

‡ **best-val vs full-eval 구분(§6-②)**: SS2D v6 의 best-val(최고 epoch 시점)은 35.96/0.0081 이나 **full-eval(7270 평균)은 34.81/0.00906** 이 정식. v6_3 의 36.05 도 best-val 로 추정되며 **full-eval PSNR 은 옛 머신에만 존재**(이 머신에 v6_3 ckpt 없음 → 로컬 재계산 불가). 따라서 U-Net(full-eval 34.66) 대비 **SSIM 추월은 견고**(+0.0066)하나 **PSNR 추월 마진은 작을 것**(best-val↔full-eval 혼합 주의).

---

## 4. v7_titan (384) 트랙 — 평가 재설계 + 재학습

### 4.1 평가 metric 재설계 (05-22)
- **동기**: 배경 부풀림(발견 ②-(1))을 학습/평가 양쪽에서 차단.
- **brain mask**: `Otsu(non-zero)×0.4 + largest CC` (no erode/fill), `dataloader_h5_v5.py:243` 하드코딩. 후보 7종 자동비교 통과율 best(82.6%).
- **weighted composite**: `0.5·SSIM_m + 0.3·min(PSNR_m,40)/40 + 0.2·max(0,1−min(NMSE_m,1))` — best/EarlyStop 기준.
- **loss**: brain-mask L1 + brain-mask SSIM_loss (학습 압력을 brain 영역에 집중).

### 4.2 ETER v7_titan — 완주 (비교 baseline)
50ep scratch 완주, early stop 없이 단조 상승:
| epoch | 5 | 10 | 15 | 25 | 35 | 45 | **50 (best)** |
|---|---|---|---|---|---|---|---|
| composite | 0.8881 | 0.8985 | 0.9036 | 0.9080 | 0.9111 | 0.9124 | **0.9127** |
| SSIM_m | 0.8903 | 0.8966 | 0.9019 | 0.9060 | 0.9074 | 0.9081 | **0.9084** |
| PSNR | 32.58 | 33.52 | 33.82 | 34.13 | 34.45 | 34.57 | **34.59** |

ckpt: `logs/ETER_ViT_R4_brain384_v7_titan/eter_vit_best.pt` (nmse 0.0044 / l1 9.5179).

### 4.3 SS2D v7_titan — 재학습 정상화 (05-31)
- 과거 SS2D는 DDP로 ep10만 돌고 중단(이후 BS8 OOM, GPU 다운). DDP 산출물은 `_ddp_archive/` 격리.
- **scratch 재시작** + **true checkpoint resume**(full-state `ss2d_vit_last.pt`: model/optim/sched/scaler/RNG/step → LR 연속, unit-test byte-identical PASS) + **auto-restart supervisor**(setsid, max_retry=50).
- 조건: BS=6, 50ep, VAL_EVERY=2, patience=50(→사실상 완주), masked composite 기준.
- **공정성**: 양측 scratch·단일GPU·50ep·384·동일 loss/metric. 차이 = sequence model(Mamba+DC vs GRU) + BS(6 vs 8).
- 데이터: train 65028 / val 7334 slice (NVMe).

### 4.4 SS2D v7_titan — 현재 진행 (2026-06-02 05:32 UTC)
| epoch | 2 | 4 | **6 (최신 val)** |
|---|---|---|---|
| composite | 0.8906 | 0.8945 | **0.8988** |
| SSIM_m | 0.8897 | 0.8927 | **0.8979** |
| PSNR | 32.96 | 33.27 | **33.49** |

- 현재 **epoch 7/50, 41%** 진행 중. 재시작 0회, GPU0 100%/82°C, 에러 없음.
- **★ 조기 우위**: SS2D **ep6 SSIM_m 0.8979 > ETER ep10 0.8966** (ETER ep5=0.8903). 동일 epoch 기준 SS2D가 앞섬 → v6의 SS2D>ETER 재현 신호.
  - 단, 초기 epoch + val 빈도 상이(SS2D 2 / ETER 5)라 **확정은 완주 후**.
- **ETA**: epoch당 ~5.7h(1.9s/batch × 10838), 50ep ≈ **~12일 → 완료 ~6/13**. true-resume가 중단 내성.

---

## 5. 현재 상태 & 진행 중 작업 (2026-06-02)

| 항목 | 상태 |
|---|---|
| SS2D v7_titan 학습 | **진행 중** epoch 7/50, ETA ~6/13 |
| ETER v7_titan | 완주(0.9127/0.9084), 비교 baseline 확정 |
| U-Net baseline ckpt | **복원·검증 완료**(2026-06-02, §6-①) — `models/pretrained/brain_leaderboard_state_dict.pt` |
| ETER baseline 분석(eval CSV+시각화) | **실행 가능·미착수** — U-Net 확보됨. 384/masked/v7_titan 로딩 스크립트 신규 작성 필요 |
| GPU1 | 12일간 **유휴**(단일GPU 설계) |
| v6_4 (VGG perceptual) | 미착수 |
| v6_3 (320) | ckpt·로그가 이 머신에 없음(옛 8GB 머신) → ETER v6_3 결과 확인·재평가 모두 불가(§6-⑤) |

---

## 6. 문제점 / 미해결 이슈

> 식별된 문제·리스크·정합성 결함. 해결된 항목은 [해결됨] 표기.

**① U-Net baseline checkpoint — [해결됨 2026-06-02]**
v6 시절 0.8858 비교에 쓰던 `brain_leaderboard_state_dict.pt` 가 cleanup 으로 소실되어 시스템 전체 부재였음. **2026-06-02 fastMRI 공식**(`dl.fbaipublicfiles.com/fastMRI/trained_models/unet/`)에서 **재다운로드·검증 완료** → `models/pretrained/` (1.85GB, **496.4M params, chans=256, raw state_dict**, 프로젝트 `Unet` 로더와 정확히 일치). 남은 caveat: U-Net 은 320-native 라 v7_titan(384) 평가 시 domain-shift(§6-⑨).

**② SS2D v6 PSNR 불일치 — [규명: best-val vs full-eval]**
`presentation §6.1`=35.96/NMSE0.0081 은 **best-val**(최고 epoch 시점) 값, `visual_metric_gap_v6`·`v6_2` 의 full-eval(7270)=**34.81±2.78/0.00906** 이 정식(SSIM·L1 일치). → §3.4 를 full-eval 기준으로 정정. **함의**: v6_3 의 36.05 도 best-val 로 추정 → "U-Net 대비 +1.39 dB 추월"은 best-val↔full-eval 혼합. **SSIM 추월(0.8924 vs 0.8858)은 견고하나 PSNR 추월 마진은 작을 것**. 단 **v6/v6_3 ckpt 가 이 머신에 없어**(옛 8GB 머신) 정확 마진은 ckpt 회수 후에만 재계산 가능.

**③ INDEX.md 깨진 링크**
`INDEX.md` 가 `SS2D_v1_analysis.md`·`eter_8gb축소.md`·`scheduler_change.md` 를 링크하나 셋 다 **2026-05-20 cleanup 에서 삭제됨**(내용은 ss2d_v4/v5_changes.md·presentation_overview에 흡수). 현재 dead link.

**④ v6 ↔ v7_titan 척도 차이 — [결함 아님, 단계 경계]**
v6=raw SSIM, v7_titan=masked SSIM 인 것은 버그가 아니라 **프로젝트 두 단계의 경계**다: **v6 이전 = "복원" 단계**(8GB·320·ViT-Small, 원본 ETER-Net 재현 목표) / **v7_titan 이후 = "향상" 단계**(TITAN·384·ViT-Base, 성능 향상 목표). 환경·목적·평가척도가 의도적으로 달라 **0.8913 ↔ 0.9084 를 같은 축에 두는 것이 무의미**. 비교는 **트랙 내부**에서만(SS2D vs ETER, U-Net vs ours), 트랙 간은 "다른 자" 명시.

**⑤ v6_3 결과 미문서화 + ckpt 부재**
presentation(05-28)은 ETER v6_3 "진행 중"으로 기록, 이후 완료·지표 문서 없음. 게다가 **v6_3 ckpt·logs 가 이 머신에 없음**(스크립트/config만 존재, 결과는 옛 머신) → 여기서 확인·재평가 불가. v6_3 채택 확정은 옛 머신 산출물 회수 후.

**⑥ v7_titan EarlyStopping 사실상 비활성**
patience=50인데 VAL_EVERY=2 → 50ep 동안 val-check가 **25회뿐** → patience(50)에 도달 불가 → **조기종료가 구조적으로 안 일어남**. ETER와 동일 조건으로 50ep 완주시키려는 **의도된 설계**지만, 과적합·발산해도 자동 중단 장치가 없음 → "epoch10 수동 중간점검"으로 보완. (현재 ep6까지 단조 상승.)

**⑦ GPU1 12일간 유휴**
v7_titan은 단일GPU 설계(DDP는 effective-batch confounder + 과거 불안정으로 폐기). SS2D 학습 ~12일 동안 TITAN 1장이 0%. 평가/시각화(ETER 분석 등)를 GPU1에서 병행할 여지 있음.

**⑧ brain mask 이상치 ~17.4% 미해소**
Otsu×0.4 mask 가 ratio<10%(strict해 dim gray matter 누락) 또는 >55%(low-contrast slice에서 threshold가 noise floor 이하) sample 을 ~17.4% 남김. "학습 noise로 흡수" 가정이나 전체 73k 분포 재확인 미완료.

**⑨ U-Net 을 384에서 평가할 때 정합성**
U-Net 은 320-native + fastmri `UnetDataTransform`(z-score) 별도 파이프라인. v7_titan(384, H5 dataloader)과 공정 비교하려면 입력 재구성 + 해상도 domain-shift 감수 → 어느 쪽이든 U-Net 에 불리한 caveat.

**⑩ 외부 의존성 취약**
SS2D 추론은 `mamba_ssm` CUDA 커널 필수(현 2.2.2). 환경 변경 시 SS2D 로드 불가. wandb 로컬 run 대부분 cleanup으로 삭제(클라우드 sync 의존) → 계정 손실 시 gradient histogram 등 raw 복구 불가.

**⑪ AXFLAIR 표기 부정확**
실제 혼합 contrast인데 단일 표기 → contrast별 성능 편차가 평균에 묻힘(per-contrast 분석 미수행).

**⑫ presentation_overview 범위 한계**
"발표용 통합본"이 v6_3(05-28)에서 멈춰 v7_titan 트랙 전체 미수록(본 문서가 보완하나 발표 자료 자체는 갱신 필요).

---

## 7. 향후 과제

**단기**: SS2D v7_titan 완주(~6/13) → ETER head-to-head 확정(384에서도 SS2D>ETER 검증). epoch10 중간점검은 조기 우위로 통과. U-Net 확보됨 → ETER v7_titan vs U-Net 분석 실행 가능.
**중기**: 학습형 sensitivity map(현 ACS-only가 DC 천장); DC iteration 1→2~4; ETER+DC 통합; v6_4 perceptual; (선택) 옛 머신 v6/v6_3 ckpt 회수 후 #2 정확 재평가.
**장기(범위 밖)**: adversarial/diffusion prior — L1+SSIM mean-prediction 한계 돌파.

---

## 8. 부록 — 전체 문서 인덱스 (md 31개)

### 프로젝트 문서 (23)
- **루트**: `CLAUDE.md`(코드베이스 가이드), `README.md`(개요), `docs/project_summary_2026-04-11.md`(구 루트 `PROJECT_SUMMARY.md`, 2026-04-11 기술 요약: dataloader/모델/학습/평가 호출그래프).
- **docs/ 핵심**: `presentation_overview.md`(v1→v6_3 발표본), `INDEX.md`(날짜순), `eval_metric_redesign.md`(mask+composite), `ss2d_v7_titan_changes.md`(재학습 정상화), `architecture_ETER_vs_SS2D.md`.
- **docs/ 버전사**: `ss2d_v4/v5/v6_changes.md`, `eter_v4_analysis.md`, `eter_v5/v6_changes.md`, `visual_metric_gap_v6.md`, `ss2d_eter_v6_1/v6_2_changes.md`, `tier1_tta_ensemble_negative.md`, `tier2_sharpness_plan.md`.
- **docs/ 인프라·정리**: `logs_archive.md`(v4~v6 메트릭 보관), `cleanup_log.md`(삭제 대장), `script_version_history.md`(삭제 .py 진화), `error_map_v2_masked.md`(2026-06-01 시각화 정책), `v7/README_v7.md`(TITAN 마이그레이션).

### 서드파티 README (8, 참고용 제외)
`models/vit_pytorch/...`, `models/mae/...`(README/FINETUNE/PRETRAIN/CONTRIBUTING/CODE_OF_CONDUCT), `models/hybrid_eternet/...README.md`.
