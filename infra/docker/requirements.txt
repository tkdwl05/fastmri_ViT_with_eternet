# 현재 운영 컨테이너(pip freeze 145개)에서 베이스 이미지 제공분을 뺀 델타.
# 버전은 전부 현행 그대로 — 재현성이 목적이므로 임의 상향 금지.

# --- SS2D(Mamba) CUDA 커널: prebuilt wheel 고정 -----------------------------
# 소스 빌드하면 30분+ 걸리고 nvcc/ABI 조합에 따라 실패한다. 현행과 동일한 URL.
https://github.com/Dao-AILab/causal-conv1d/releases/download/v1.4.0/causal_conv1d-1.4.0+cu122torch2.3cxx11abiFALSE-cp310-cp310-linux_x86_64.whl
https://github.com/state-spaces/mamba/releases/download/v2.2.2/mamba_ssm-2.2.2+cu122torch2.3cxx11abiFALSE-cp310-cp310-linux_x86_64.whl

# --- 모델/학습 ---------------------------------------------------------------
einops==0.8.1

# --- 데이터/수치 -------------------------------------------------------------
h5py==3.16.0
scipy==1.15.3
pandas==2.3.3
scikit-image==0.25.2

# --- 시각화 ------------------------------------------------------------------
matplotlib==3.10.7

# --- 실험 추적 ---------------------------------------------------------------
wandb==0.27.0

# --- ViT 백본 보조(hybrid_eternet/mae 경로에서 참조) -------------------------
transformers==4.35.2

# --- 논문 트랙(docx 생성) ----------------------------------------------------
pypandoc_binary==1.17

# --- 빌드 보조 ---------------------------------------------------------------
ninja==1.13.0

# --- 베이스 이미지 제공분 명시 핀 (2026-08-31 감사 후 추가) -----------------
# dataloader·학습 스크립트가 직접 import 하는데, 베이스의 conda-build/ipython
# 의존으로만 존재하던 것들. 현행 conda 버전과 동일 핀이라 설치 시 no-op —
# mri:v1 은 이 3줄 없이 빌드됐지만 내용물은 동일하다. 베이스가 바뀌어도 안전.
psutil==5.9.0
tqdm==4.66.4
pytz==2024.1
